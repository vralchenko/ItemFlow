{
  "name": "backend",
  "version": "1.0.0",
  "description": "The backend server for the ItemFlow project.",
  "main": "server.js",
  "scripts": {
    "start": "cross-env NODE_ENV=development nodemon server.js",
    "start:test": "cross-env NODE_ENV=test nodemon server.js",
    "db:init": "cross-env NODE_ENV=development node init-db.js",
    "test:init-db": "rm -f ./test-database.sqlite && cross-env NODE_ENV=test node init-db.js",
    "test:api": "playwright test"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "multer": "^2.0.2",
    "sqlite": "^5.1.1",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "@types/node": "^24.7.2",
    "cross-env": "^10.1.0",
    "dotenv": "^17.2.3",
    "nodemon": "^3.1.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
